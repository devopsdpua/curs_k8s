# Allows HTTPRoute in namespace "default" to reference Service in namespace "monitoring".
# Without this, cross-namespace backendRef to Grafana is rejected and the gateway returns 500.
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-default-httproute
  namespace: monitoring
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: default
  to:
    - group: ""
      kind: Service
