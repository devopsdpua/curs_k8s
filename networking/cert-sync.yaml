apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-sync
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cert-sync
  template:
    metadata:
      labels:
        app: cert-sync
        azure.workload.identity/client-id: "3be46062-ba6e-4f11-8766-ea78d1fab9b9"
    spec:
      containers:
        - name: cert-sync
          image: registry.k8s.io/pause:3.9
          volumeMounts:
            - name: tls-secret
              mountPath: /mnt/tls
              readOnly: true
      volumes:
        - name: tls-secret
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: wildcard-tls
