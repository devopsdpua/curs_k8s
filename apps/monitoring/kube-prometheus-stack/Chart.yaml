# Wrapper chart for kube-prometheus-stack (Prometheus, Grafana, Alertmanager, ...)
# ArgoCD will run helm dependency build + helm template from this path.
apiVersion: v2
name: kube-prometheus-stack
description: Wrapper for prometheus-community/kube-prometheus-stack
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: kube-prometheus-stack
    repository: https://prometheus-community.github.io/helm-charts
    version: "55.0.0"  # Pin to a specific version; update as needed
